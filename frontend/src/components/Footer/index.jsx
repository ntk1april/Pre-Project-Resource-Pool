import '../../App.css';
export default function Footer() {
    return (
        <footer>
            {`Using Sping Boot + ReactJS + MySQL + Tailwind CSS + Material UI © ${new Date().getFullYear()}`}
        </footer>
    )
}